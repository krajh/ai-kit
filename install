#!/usr/bin/env bash
# Legacy installer redirect - use ai-kit-install instead
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Redirect to new installer
exec "$SCRIPT_DIR/ai-kit-install" "$@"
