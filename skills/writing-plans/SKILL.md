---
name: writing-plans
description: "Use the repo’s planning/spec workflow; do not create plan docs unless requested."
---

# Planning (Repo Workflow)

Use the repo’s spec/template workflow for non-trivial tasks.

- Do not create `docs/plans/*` unless the user explicitly requests it.
- Do not commit anything unless explicitly asked.
