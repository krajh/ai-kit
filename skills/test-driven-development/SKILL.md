---
name: test-driven-development
description: "Optional TDD guidance; use only if the user explicitly requests strict TDD."
---

# Optional: Strict TDD

The strict superpowers TDD workflow conflicts with the repo’s default flow.

- Use only when the user explicitly requests strict TDD.
- Otherwise follow repo verification requirements (verify-loop) and testing rules.
